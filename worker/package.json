{
  "name": "platepatch-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "types": "wrangler types",
    "migrate:local": "wrangler d1 migrations apply platepatch --local",
    "migrate:remote": "wrangler d1 migrations apply platepatch --remote"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.0",
    "typescript": "^5.9.0",
    "vitest": "~3.2.0",
    "wrangler": "^4.129.0"
  }
}
